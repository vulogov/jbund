extern crate log;
use easy_error::{Error};

use bundcore::bundcore::Bund;
use bund_stdlib_text_classifier;

pub fn init_stdlib() {
    // bundcore::add_stdlib("textclassifier", &bund_stdlib_text_classifier::init_lib);
}
